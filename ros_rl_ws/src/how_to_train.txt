1、启动Gazebo仿真环境（系统python）
conda deactivate
cd ./ros_rl_ws
catkin_make
source devel/setup.bash
roslaunch my_env_generator env_generator.launch

2、启动rosbridge（系统python）
conda deactivate
cd ./ros_rl_ws
roslaunch rosbridge_server rosbridge_websocket.launch

3、启动环境桥接文件（系统python）
conda deactivate
cd  ./ros_rl_ws/src/gops_ros_wrapper/scripts
python3 gazebo_bridge.py

4、启动GOPS 训练（conda）
conda activate gops
cd ./GOPS/example_train/fpisac
python3 fpisac_mlp_veh2dof_tracking_error_serial.py

nohup python3 fpisac_mlp_veh2dof_tracking_error_serial.py > train.log 2>&1 &


echo xfce4-session > ~/.xsession
sudo adduser xrdp ssl-cert
sudo systemctl enable xrdp
sudo systemctl restart xrdp
sudo systemctl status xrdp
ss -ltnp | grep 3389
